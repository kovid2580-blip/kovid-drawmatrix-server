{
    "name": "workspace-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "dev": "concurrently \"npm run dev --prefix drawmatrix_reference\" \"npm run dev --prefix server\"",
        "frontend": "npm run dev --prefix drawmatrix_reference",
        "backend": "npm run dev --prefix server",
        "lint": "npm run lint --prefix drawmatrix_reference",
        "format": "npx prettier --write ."
    },
    "devDependencies": {
        "concurrently": "^9.2.1"
    }
}
